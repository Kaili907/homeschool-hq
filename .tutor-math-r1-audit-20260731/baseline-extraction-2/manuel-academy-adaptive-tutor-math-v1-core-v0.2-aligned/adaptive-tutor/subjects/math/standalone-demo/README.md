# Standalone Demonstration

Open `index.html` directly in a modern browser. No server, build, account, camera, microphone, or network connection is required.

The demo intentionally uses an ungraded sample. It records evidence only in page memory and resets when the page closes or reloads.
