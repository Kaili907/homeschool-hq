# CYCLE claim — SESSION 3

- **Session:** SESSION 3
- **Cycle:** MT-1 — scripted walkthroughs + voice (Tutor-Addendum-v2-1 §MT-1)
- **Branch:** mt1-walkthroughs
- **Worktree:** ../hq-mt1 (dev port 5176)
- **Scope (grades 3/4/6 ONLY):** `explain()` for every g3/g4/g6 generator (33
  skills) with the question's actual numbers interpolated; wrong-answer
  walkthrough flow (offer → tap-to-advance stepped walkthrough with step
  highlights → fresh retry at same difficulty, reduced mastery weight);
  always-available "explain this one" on every question incl. correct answers
  (operator-requested addition); speechSynthesis voice with per-profile voice
  picker + rate + global mute, text always displayed; escalation rule (3+
  same-skill walkthroughs/session or 5+/week → Needs-Dad flag in Grown-Ups,
  skill gated from practice until Dad clears). Teens text-first.
- **Data model:** new fields are OPTIONAL with runtime defaults. NO
  schemaVersion bump. If a breaking schema change proves unavoidable: STOP.
- **Out of scope:** MT-2 (no API/Claude anything), MT-3 (voice input), MA, MS,
  MM, M4/HS explanations, any new question generators.
- **Out of scope for me but claimed by others (non-overlapping):** SESSION 1
  ma-assessment-player (HS assessment player), SESSION 2 m4-hs-mode (HS mode).
- **Base:** master @ 1f476e1, tag v2.0-m3 present.
- **Claimed:** 2026-07-23 (single machine, no remote; local branch containing
  this file = claim of record. No push.).
