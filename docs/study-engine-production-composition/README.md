# Session 16 final handoff

## Decision

**PASS WITH CONDITIONS**

The host now invokes the sole production composition root, the canonical 17-slot registry is runtime-branded and fail-closed, verified guardian launch and opaque learner-session verification are implemented locally, every available Session 13 durable academic adapter has a production assembly path, lifecycle epochs quarantine stale work, and production bundles force authenticated same-origin provider gateways.

Production Study remains intentionally unavailable. Session 17 still owns the delivery provider and receipt validator, and no approved production academic runtime yet replaces the checksum-frozen RC1 runtime's synthetic learner identity. The host therefore supplies no invented authority, incomplete registry, or synthetic runtime. This is an intentional production safety result, not an in-memory fallback.

## Custody

- Repository: `C:\Users\aemanuel\homeschool-hq`
- Worktree: `C:\Users\aemanuel\homeschool-hq\.worktrees\study-engine-production-composition`
- Branch: `integrate/study-engine-production-composition`
- Exact base: `e788d4ad12333b13d9c6fe4d014536d84c641331`
- Runtime: Node `v22.23.1`
- Migration created but not applied: `20260801160000_academy_study_verified_identity.sql`

The branch was created from the exact required commit in a new worktree. The Session 15 worktree was not modified. RC1 and Tutor Core were not edited.

## Results

- Production registry: enforced, immutable, 17 exact slots; incomplete runtime remains `not-ready`.
- Host invocation: actual call from `src/App.tsx`; feature-only activation is impossible.
- Guardian launch: authenticated bearer plus server-derived household, membership, relationship, permission, credential, and learner authority.
- Learner session: short-lived, one learner/household, opaque browser reference, digest-only persistence, verification per capability, expiration, revocation, and rotation.
- Staff: disabled with structured `staff-authorization-unavailable`; no repository-approved staff governance model exists.
- Sentinel: frozen sentinel remains only in explicit development-preview/test custody; production runtime branding rejects it and the production bundle omits it.
- Durable ports: all eight Session 13 Supabase adapters are assembled into nine academic/durable registry slots; the outbox adapter satisfies proposal-store and outbox-store contracts.
- Readiness: authenticated server endpoint and bounded browser cache; actual health only; `ready | not-ready | degraded`; client projection is schema, status, and expiry only.
- Lifecycle: one authority-bound epoch with cancellation and stale-result quarantine.
- Provider boundary: production Tutor and ElevenLabs traffic is same-origin and bearer-authenticated; browser provider keys and direct provider URLs are compiled out.
- Preview: requires development build, feature flag, and explicit preview opt-in; production selection never imports local preview ports.
- Accessibility/mobile: learner-safe unavailable screen, focused heading, semantic main/heading, no console errors or horizontal overflow at 320px/390px, 44px action target.
- Dependency audit: four locks report zero vulnerabilities; the frozen RC1 runtime lock reports two high-severity Playwright browser-download advisories and must be remediated by its owning release session without changing RC1 custody here.

## Scope declaration

No push, merge, deployment, hosted migration, production feature enablement, hosted Supabase inspection, real student-data access, or external provider call occurred. All validation data was synthetic.

The remaining evidence and replacement maps are in the adjacent reports.
