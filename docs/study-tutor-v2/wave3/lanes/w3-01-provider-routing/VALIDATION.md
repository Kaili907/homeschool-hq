# W3-01 validation

R2 focused result: PASS — 12 tests, 0 failures.

Coverage includes immutable selected identity, per-provider W3-08 evaluation,
policy revision mismatch, caller-declared eligibility rejection, immutable
availability matching, separately costed primary/failover attempts, catalog and
model-alias drift, decimal micros beyond `Number.MAX_SAFE_INTEGER`, duplicate
physical-attempt rejection, capability/safety/latency/cost filters, schema
closure, and the Study authority boundary.

The strict source and test-tree TypeScript configurations pass. See the R2
repair validation handoff for the combined four-lane command and result.
