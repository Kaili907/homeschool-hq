# Learner release browser E2E evidence

This directory records production-build/browser evidence for the immutable
repair assembly. It is not a deployment record and makes no deploy claim.

`playwright-results.json` is generated by the learner-release Playwright config.
`R1-ACCEPTANCE.md` records the immutable inputs, exact commands, passing proof,
blocking proof, and final classification.
