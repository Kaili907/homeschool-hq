# Netlify boundary

Local configuration declares the Vite build, `dist` publish directory, `netlify/functions`, Node 22, Study proxy routes, readiness/session/runtime endpoints, and the adult-review worker schedule before the SPA fallback.

Provider keys remain server-side. Browser bundles and responses must not contain provider/service credentials.

Hosted Netlify branch, function schedule, environment, and deployment state were not authenticated or modified in this session. Current hosted Netlify state is **UNKNOWN**.
