# Remaining blockers

Final decision: **BLOCKED**.

1. The deployed host does not compose the complete immutable RC1/Tutor/safety/adult-review application behind the verified identity gateway. `App.tsx` proves the opaque server-derived runtime exchange and owned lifecycle cancellation, but it exposes a minimal verified-runtime host rather than the complete Study experience.
2. The canonical branded 17-port Session 16 production registry is not fully instantiated. Default scheduled worker, health, manual delivery, and related Netlify paths do not supply the complete Session 17 v2 recipient/outbox/lease/attempt/receipt/monitoring composition and therefore remain fail-safe not-ready/503.
3. Authenticated exact-project access was unavailable. Foundation equivalence, migration-ledger absence, Study-object absence, collision checks, compatibility counts, and final grant/PostgREST assumptions were not re-preflighted. Hosted baseline and migration authorization remain blocked.
4. The full Node 22 gate is not complete: the frozen RC1 runtime typecheck/browser suite lacks its immutable package-local browser test dependencies, and no browser harness exists for the authenticated reconciled host. Manual multi-browser and assistive-technology coverage is also outstanding.
5. Staff authorization remains `staff-authorization-unavailable`; no approved repository staff permission model exists.
6. The frozen RC1 Playwright development toolchain has two high advisories. They are not production-reachable, but a future RC still requires explicit risk acceptance or a dependency update and re-audit.

These blockers prohibit `PASS` and `PASS WITH CONDITIONS`. They do not invalidate the committed local migration/security corrections, and they do not authorize hosted work.
