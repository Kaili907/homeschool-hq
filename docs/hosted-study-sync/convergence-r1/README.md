# Hosted Study sync convergence R1

## Classification

`BLOCKED`

This candidate safely converges the reusable lane artifacts and adds a real,
feature-gated DB-RPC/IndexedDB coordinator. It is not production- or
hosted-staging-ready because the authoritative input contracts cannot carry all
audited Family Pilot authority state and the required 28 scenarios have not run
through one real convergence adapter stack.

No hosted Supabase call, hosted SQL execution, deploy, Netlify production
change, Tutor activation, master merge, or service-role browser capability was
performed.

## Candidate scope

- exact input commits imported without merging stale branch snapshots;
- IndexedDB remains the immediate durable learner authority;
- memory-only adult/Study authorization bridge adapted to the DB RPC client;
- exact `academy_study_sync_hydrate_v1` and
  `academy_study_sync_write_v1` request/response parsers;
- payload-free bounded queue metadata in IndexedDB;
- CAS/idempotency, conservative hydrate, privacy, readiness, and three-stage
  rollout gate;
- local PGlite migration/RPC validation and production browser bundle scan.

See `contract-gaps.md` for the blocking evidence.
