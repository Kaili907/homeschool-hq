# Family Cloud Sync R1 staging activation report — 2026-08-15

## Ruling

`STAGING_ACTIVATION_BLOCKED`

The authorized staging database is migrated and its deployed RLS/RPC boundary
passes the real hosted security probes. The web application cannot yet be
truthfully activated: the feature flag does not compose a live transport, and
the emitted browser build contains neither the staging Supabase endpoint nor a
browser key. No Netlify deploy was created.

## Custody and targets

| Field | Result |
| --- | --- |
| Source SHA | `1dfd0452cde90078c453ed3e4301bb63a3c02d83` |
| Source tree | `57cf2e45d17832ce38b3027ca0794b42673d3f62` |
| Worktree | `mac-family-cloud-sync-r1-staging-activation` |
| Branch | `mac/family-cloud-sync-r1-staging-activation` |
| Staging project | `manuel-academy-staging` |
| Authorized staging ref | `fqzcxrkvpaivpnzdbuol` |
| Forbidden production ref | `ymtvzmqhfvwjtxjdmybs` |
| Target guard | staging accepted; production mechanically refused |
| Supabase link | exact staging ref verified before every hosted operation |
| Production contact | none |

The CLI was authenticated without exposing an access token. No database
password, refresh token, service-role key, secret key, or browser-key value was
printed or committed.
After all hosted operations finished, the supported CLI unlink command removed
the untracked local link metadata to reduce accidental future hosted contact;
this did not mutate the staging database.

## Migration activation

The initial remote ledger was empty. The complete first dry-run proposed the
57 source-candidate migrations in order, with no seed or role changes. All 57
were applied to staging only. A real first-link probe then found one constraint
failure: an empty Safety hold set produced `clear`, a null clear timestamp, and
a non-null clearer.

No historical migration was edited. The additive repair
`20260815180000_academy_family_session_safety_clear_shape_r1.sql` normalizes
that impossible half-pair before the existing constraint runs. Its isolated
remote dry-run proposed exactly one migration; it was inspected, replayed
locally, applied to staging, and verified.

Final state:

- local migration inventory: 58 unique migrations;
- remote migration inventory: the same 58 versions;
- final remote dry-run: up to date, with zero migrations, seeds, or roles;
- empty-database replay: 58/58 applied;
- required Family response, plan, and session-authority relations: RLS enabled
  and forced as designed;
- four public Family Sync R2 RPCs: authenticated execution present and
  anonymous execution absent;
- response, authority, and Family Plan CAS domains: installed; response CAS
  exercised against the hosted database.

## Real hosted database probes

The checked-in probe at `scripts/family-cloud-staging/security-probes.sql`
uses synthetic staging-only identities inside one transaction and ends with
`ROLLBACK`. A separate read after the probe confirmed zero residual synthetic
households, learners, and Auth users.

| Probe | Hosted result |
| --- | --- |
| Valid first link | `imported` |
| Valid sibling first link | `imported` |
| Anonymous household/learner read | denied by ACL/RLS |
| Anonymous hydrate execute | denied |
| Wrong household | `unavailable`; wrong household row count 0 |
| Wrong learner | `denied / study-session-invalid` |
| Sibling-private access | own row 1, sibling row 0 |
| Invalid identity | household rows 0, learner rows 0, RPC `unavailable` |
| Stale response CAS | `revision-conflict`, server revision retained |
| Unexpected response field | `invalid-write` |
| `answerKey` | `invalid-write` |
| `correctAnswer` | `invalid-write` |
| adult rubric | `invalid-write` |
| Tutor transcript | `invalid-write` |
| Parent PIN | `invalid-write` |
| refresh token | `invalid-write` |

## Auth configuration

`AUTH_DASHBOARD_CONFIGURATION_REQUIRED`

The installed CLI can only push a complete local `supabase/config.toml`; this
repository has no such file and no safe read/merge mechanism. A broad config
push was therefore not attempted. In the Supabase Dashboard for
`manuel-academy-staging` (`fqzcxrkvpaivpnzdbuol`) only, enter:

- Site URL: `https://family-pilot-cloud-r1--manuel-academy.netlify.app`
- Redirect allow list:
  - `https://family-pilot-cloud-r1--manuel-academy.netlify.app`
  - `https://family-pilot-cloud-r1--manuel-academy.netlify.app/family-pilot`

Do not enter a production URL in staging. The Family account confirmation
redirect generated by the application targets the exact `/family-pilot` URL.

## Parent account UX

The source candidate had sign-in only. This activation change adds the smallest
provider-managed email/password account-creation surface to the existing
Family Cloud Auth coordinator, including confirmation-required handling and a
`/family-pilot` confirmation return. Tests prove that the password is not
stored in device-link storage.

This UI remains unreachable in the current built application because no
`FamilyCloudAuthRuntime` is injected by either root composition. A newly
created Auth user also has no product path that creates the required household,
guardian membership, learners, or ephemeral Study grants.

## Browser build and deploy ruling

A process-local build used these existing flags and no committed environment
file:

- `VITE_FAMILY_PILOT_ENABLED=true`
- `VITE_FAMILY_PILOT_HOSTED_SYNC_ENABLED=true`
- `VITE_STUDY_ENGINE_ENABLED=true`
- `VITE_FAMILY_PILOT_TRUSTED_SCORER_ENABLED=false`
- staging `VITE_SUPABASE_URL`
- staging publishable browser key

The production build and answer-authority audit passed. The asset audit then
found zero occurrences of the authorized staging project ref and zero
publishable-key prefixes. This proves the requested hosted-sync flag is not
consumed by the runtime graph. No production project ref, secret-key prefix,
database password, answer key, correct answer, or scoring guide was present.
The literal service-role/Tutor strings that remain are denylist or explicit
`false` backup-privacy assertions, not credentials or payloads.

The runtime-isolation audit independently reported
`acceptedFamilyPilotAdapters: []`. Deploying this build under a cloud-live name
would be misleading and could not perform the required Device A to B test.
Consequently:

- Netlify site ID: `5eca8778-df0b-4d44-abbd-2a488f987dbe`;
- deploy ID: not created;
- immutable deploy URL: not created;
- alias URL reserved by the request but not deployed:
  `https://family-pilot-cloud-r1--manuel-academy.netlify.app`;
- production Netlify: unchanged; no Netlify mutation was attempted.

## Blocking implementation boundary

The following production implementations/composition are absent, not merely
misconfigured:

1. neither `App.tsx` nor `FamilyPilotWebApp.tsx` constructs or injects
   `FamilyCloudAuthRuntime`;
2. `FamilyCloudRemoteDirectoryPortR1` has no browser implementation or safe
   endpoint for resolving the required per-learner ephemeral grant material;
3. `FamilyCloudCheckpointRepositoryR1` has no canonical browser-store
   implementation for atomic staged publish across localStorage and IndexedDB;
4. `HostedSyncEphemeralAuthorization` has no Supabase authenticated provider
   implementation for the four deployed RPCs;
5. both `FAMILY_HOSTED_SYNC_CONVERGENCE_R1.enabled` and
   `HOSTED_SYNC_PRODUCTION_ACTIVATION.enabled` remain hard-coded false;
6. no trusted onboarding path creates a new account's household, membership,
   learner rows, and grants.

These gaps block account login/first link, truthful cloud status, real browser
network verification, Device A/B/C tests, School Plan sync, conflict/offline
browser proofs, and hosted backup verification. The local three-device emulator
proof remains valid but is not represented as hosted evidence.

## Verification summary

Passed:

- migration collision check: 58 unique;
- empty-database migration replay: 58/58;
- Hosted Sync DB/RPC tests: 26/26;
- Hosted Sync R2 tests: 38/38;
- focused Family Cloud/convergence tests: 85/85;
- Family Cloud Auth tests: 16/16;
- preflight guard tests: 11/11;
- local Family Pilot Chromium journey: 14/14;
- local three-device emulator E2E: 1/1;
- TypeScript;
- Family Pilot production build;
- browser answer-authority audit;
- database security audit;
- Family Pilot release/runtime-isolation audits.

The broad `npm test` command has one unrelated existing failure in
`tests/study-deployment-env-preflight.test.js`: the integrated static preflight
classifies the Netlify functions directory as malformed. The web-release audit
also reports two `parentPin` identifier literals in the local Family Pilot
bundle; inspection shows these are local PIN/denylist vocabulary, not embedded
PIN values or cloud payloads. Neither finding caused the hosted transport
blocker above.

Release invariants reverified: 90 courses, 8,292 lessons, 699 assessments, all
10 subjects, supported grades 3/4/5/7/8/9/10/11/12, Grade 6 excluded, 8,292
production bindings, 8,292 learner materials, zero adult-only leaks, one Study
Engine composition, one LearnerResponseRuntime, and one canonical local
IndexedDB authority.

The requested `3,108 production-depth lessons` subset is not represented by a
current checked-in counter or auditor, so it is reported as not independently
reverified rather than inferred. The stronger full-population binding/material
gates did verify all 8,292 admitted lessons.

## Required continuation

Implement and review the six missing staging composition pieces above, apply
the exact Dashboard Auth values, then rebuild. The next build must contain the
staging endpoint and publishable key, pass the asset audit, and only then be
deployed to the non-production alias for the real Device A to B to A proof.
