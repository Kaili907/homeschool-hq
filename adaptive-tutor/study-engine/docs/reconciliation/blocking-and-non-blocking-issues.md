# Blocking and Non-Blocking Issues

The machine issue register is [`issues.v1.json`](../../reconciliation/issues.v1.json). It is authoritative for IDs, status, evidence, and required resolution.

## BLOCKER

- `CORE-001` — Actual Tutor Core v0.2 package/manifest/bytes unavailable.
- `EVIDENCE-001` — No verified Core bridge for mastery, misconceptions, prerequisites, uncertainty, reteaching, or adult evidence.
- `SKILL-001` — Study-UX fixtures lack canonical SkillId/Core item binding.
- `ACCOMMODATION-001` — Requiredness and typed operational accommodation effects are missing.
- `CAP-001` — Session 2 hold can preserve a duration above a hard cap.
- `PRIVATE-AUDIENCE-001` — Parent-only material cannot be widened to authorized-adults-only.
- `PARENT-ACCEPT-001` — Recommendation acceptance rewrites hard guardrails.

The adult-private body duplication, unsupported-version reset/restamp behavior, learner-facing free-text rendering, and Romeo credential/URI boundary are also treated as assembly-stopping safety work even where the central register labels the planned replacement `REQUIRED BEFORE FINAL ASSEMBLY`.

## REQUIRED BEFORE FINAL ASSEMBLY

This class includes canonical transition/event mapping, exact resume, real segment binding, two-axis timer/break mapping, typed accessibility fields, recommendation lifecycle, retry-not-before, queue result return, household zone source, adult-private projection, extension allowlists, state allowlists, parent command idempotency, Romeo due-date/security mapping, registry audience capability, and all unsupported-version quarantine gaps.

These issues do not prevent publication of this audit; they prevent an integration/final-assembly decision.

## SAFE ADAPTER

- Widen provisional ID validators without changing bytes.
- Map S2 grade bands to Session 1 bands.
- Normalize ZIP entry separators only in readers.
- Map review interval days to Session 1 identifiers.
- Project calendar blocks using stable source references.
- Isolate browser dependencies and use a dependency-neutral JSON sidecar.

Safe adapters still require tests and must quarantine lossy/unknown inputs.

## DOCUMENTATION ONLY

- Logical package IDs are not yet declared in publishable manifests.
- Final Tutor Core hash/export inventory must be added when the actual package arrives.
- Existing privacy-test claims should be described accurately as structural/keyword checks, not complete DLP.

## DEFERRED PRODUCTION CONCERN

Production persistence, authentication, authorization, encryption, retention/deletion, quarantine retention, observability, rollout, deployment, calendar host policy, and Romeo intake governance remain outside Session 5. Their seams are recorded; no production work was done.

